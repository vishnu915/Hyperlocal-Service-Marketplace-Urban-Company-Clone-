# Copy to .env.local or .env and/or set these variables when running locally
# Vite requires variables to start with VITE_ to be exposed to the client.
VITE_API_BASE_URL=http://localhost:8000/api
VITE_API_PROXY=http://localhost:4000

# If your backend runs on a different port, change the URL above.
# Example for production: VITE_API_BASE_URL=https://api.myapp.com/api
